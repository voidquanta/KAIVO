package com.kaivo.app.ui.theme

import androidx.compose.ui.graphics.Color

// KAIVO is strictly black & white — no accent colors, no gradients.
val PureWhite = Color(0xFFFFFFFF)
val PureBlack = Color(0xFF000000)

// Subtle grays used only for card surfaces / dividers / disabled states,
// never as a "color" — these keep the palette feeling premium instead of flat.
val LightSurface = Color(0xFFF5F5F5)
val LightBorder = Color(0xFFE0E0E0)
val LightSecondaryText = Color(0xFF6B6B6B)

val DarkSurface = Color(0xFF141414)
val DarkBorder = Color(0xFF2A2A2A)
val DarkSecondaryText = Color(0xFFA0A0A0)
